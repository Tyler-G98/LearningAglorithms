<UserControl x:Class="LearningAglorithms.Controls.UC_PlayingCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="100"
             MaxHeight="150" MaxWidth="100">
    <Border BorderBrush="Black" BorderThickness="5" CornerRadius="5" Background="AntiqueWhite">
        <Grid>
            <!-- Centered TextBlock for the card number -->
            <TextBlock Text="{Binding CardNumber, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="24" FontWeight="Bold"/>
        </Grid>
    </Border>
</UserControl>